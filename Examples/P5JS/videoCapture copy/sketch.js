
function setup() {
  createCanvas(400, 300);
  capture = createCapture(VIDEO);
  // capture.size(300, 200);
  //capture.hide();
}

function draw() {
  background(255);
  //image(capture, 0, 0, width, height);
}
